### 📌 Branch Overview

This repository contains local sub system binary logic and binary downloadable links in teh assests:

####  `branch/setup-binary`
Contains tooling and configuration required to build and package the project into a standalone binary for deployment.
